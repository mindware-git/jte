# Project Arena
Godot 4 for mobile 2d game

## Spec & Test driven project
AI first, so everything should generate spec -> test -> implementation.

## Testing
GUT
`$ godot --headless --path . -s addons/gut/gut_cmdln.gd`

## Naming Convention
- Use `bluewood` style for variable names (e.g., `bluewood_account`)
- Exception: Character names like `samzang` are allowed as-is

## Documentation
- Architecture documents are available in `docs/` folder
- Key documents: ARCHITECTURE.md, BATTLE_ARCHITECTURE.md, SAVE_LOAD_ARCHITECTURE.md
- Refer to them when needed
