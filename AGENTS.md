# Project Arena
Godot 4 for mobile 2d game

## Spec & Test driven project
AI first, so everything should generate spec -> test -> implementation.

## Testing
GUT
`$ godot --headless --path . -s addons/gut/gut_cmdln.gd`
